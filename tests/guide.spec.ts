import {test,expect} from '@playwright/test'; test('role and completion persist',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:'팀원으로 첫 PR 보내기'})).toBeVisible();await page.getByRole('button',{name:'나는 조장입니다'}).click();await expect(page.getByRole('heading',{name:'안전한 협업 흐름 만들기'})).toBeVisible();await page.getByRole('button',{name:'나는 팀원입니다'}).click();await page.getByRole('checkbox').check();await page.reload();await expect(page.getByRole('checkbox')).toBeChecked()}); test('problem search',async({page})=>{await page.goto('/');await page.getByLabel('가이드 검색').fill('충돌');await page.getByRole('button',{name:'문제가 생겼습니다'}).click();await expect(page.getByRole('heading',{name:'충돌 발생'})).toBeVisible()});
