import { useEffect, useMemo, useState } from 'react';
import { lessons, problems, templates, terms, type Lesson, type Role } from './data/content';

const download = (name: string, body: string) => { const link = document.createElement('a'); link.href = URL.createObjectURL(new Blob([body], { type: 'text/markdown' })); link.download = name; link.click(); URL.revokeObjectURL(link.href); };
const practice: Record<string, { change: string; check: string }> = {
  branch: { change: 'Issue #1 “팀원 소개 파일 추가”를 위해 feature/1-member-intro 브랜치를 만들었습니다.', check: 'Current branch가 main이 아니라 feature/1-member-intro인지 확인합니다.' },
  edit: { change: 'members/member-intro.md에 “안녕하세요. test-only 실습 참여자입니다.” 한 줄을 추가했습니다.', check: '파일 경로와 변경 목적이 Issue와 같은지 확인합니다.' },
  commit: { change: '“Add member introduction example” Commit을 만들었습니다.', check: 'Commit 메시지만 읽어도 변경 내용을 알 수 있는지 확인합니다.' },
  push: { change: 'Push 후 GitHub의 Pull requests 화면에서 새 브랜치를 확인할 수 있습니다.', check: 'Commit만 한 상태가 아닌지, GitHub 웹에도 브랜치가 보이는지 확인합니다.' },
  pr: { change: 'base: main, compare: feature/1-member-intro로 PR을 만들고 Issue #1을 연결했습니다.', check: 'base와 compare의 방향이 뒤집히지 않았는지 확인합니다.' },
  review: { change: 'Files changed에서 members/member-intro.md 한 파일과 추가한 한 줄을 검토했습니다.', check: '무관한 파일이나 민감정보가 섞이지 않았는지 확인합니다.' },
  finish: { change: 'PR #2를 병합하고 feature/1-member-intro 브랜치를 정리했습니다.', check: 'Merged 상태와 최신 main을 확인한 뒤에만 브랜치를 정리합니다.' },
};

function Visual({ lesson, open }: { lesson: Lesson; open: (src: string, alt: string) => void }) {
  if (!lesson.visual) return <div className="visual missing"><strong>이 단계는 화면보다 작업 확인이 중요합니다</strong><small>{lesson.tool}에서 안내 문장을 따라 진행하세요.</small></div>;
  const filename = lesson.visual.startsWith('focus-') ? `${lesson.visual}.png` : lesson.visual.replace('.png', '-annotated.png');
  const src = `${import.meta.env.BASE_URL}annotated/github/${filename}`;
  const alt = `${lesson.title}: 눌러야 할 위치를 표시한 개인정보 마스킹 GitHub 화면`;
  return <figure className="visual captured"><button className="image-button" onClick={() => open(src, alt)} aria-label={`${lesson.title} 이미지 확대 보기`}><img src={src} alt={alt} onError={(event) => { event.currentTarget.closest('figure')?.classList.add('broken'); }} /></button><figcaption><button onClick={() => open(src, alt)}>확대 보기</button> · 개인정보 마스킹본</figcaption></figure>;
}

function Simulation() {
  const [answer, setAnswer] = useState<string | null>(null);
  const choices = [['commit', 'Commit만 하고 팀원에게 완료라고 알린다'], ['push', 'Push origin을 눌러 GitHub에 브랜치를 올린다'], ['main', 'main 브랜치에서 파일을 다시 수정한다']];
  return <section className="simulation"><p className="eyebrow">1분 확인</p><h3>Commit을 마친 뒤, 팀원이 변경을 보게 하려면?</h3><div>{choices.map(([id, label]) => <button key={id} className={answer === id ? (id === 'push' ? 'correct' : 'wrong') : ''} onClick={() => setAnswer(id)}>{label}</button>)}</div>{answer && <p role="status">{answer === 'push' ? '정답입니다. Commit은 내 컴퓨터의 기록이고, Push가 GitHub에 공유하는 단계입니다.' : '다시 생각해 보세요. 팀원이 보려면 GitHub에 브랜치를 올리는 Push가 필요합니다.'}</p>}</section>;
}

export function App() {
  const [role, setRole] = useState<Role>('member'); const [view, setView] = useState<'guide' | 'problem' | 'terms'>('guide'); const [step, setStep] = useState(0); const [query, setQuery] = useState(''); const [modal, setModal] = useState<{ src: string; alt: string } | null>(null); const [done, setDone] = useState<string[]>(() => JSON.parse(localStorage.getItem('guide-progress') || '[]'));
  const list = lessons[role]; const lesson = list[step]; const progress = Math.round(list.filter((item) => done.includes(`${role}:${item.id}`)).length / list.length * 100);
  const searchMatches = useMemo(() => query.trim() ? list.map((item, index) => ({ item, index })).filter(({ item }) => `${item.title} ${item.tool} ${item.action} ${item.why}`.toLowerCase().includes(query.toLowerCase())) : [], [list, query]);
  useEffect(() => localStorage.setItem('guide-progress', JSON.stringify(done)), [done]); useEffect(() => { setStep(0); setQuery(''); }, [role]); useEffect(() => { const close = (event: KeyboardEvent) => event.key === 'Escape' && setModal(null); window.addEventListener('keydown', close); return () => window.removeEventListener('keydown', close); }, []);
  const toggleComplete = () => setDone((items) => items.includes(`${role}:${lesson.id}`) ? items.filter((item) => item !== `${role}:${lesson.id}`) : [...items, `${role}:${lesson.id}`]);
  const find = (value: string) => value.toLowerCase().includes(query.toLowerCase());
  return <div><header><div className="brand"><b>GitHub 팀프로젝트</b><small>비전공자용 화면 중심 실습 가이드</small></div><button onClick={() => window.print()}>인쇄 / PDF</button></header><div className="layout"><aside><p className="eyebrow">START HERE</p><h1>처음이어도<br /><em>같이 만들 수 있게.</em></h1><p>GitHub Desktop으로 안전하게 작업하고, GitHub 웹에서 PR과 리뷰를 완성합니다.</p><nav><button className={view === 'guide' ? 'on' : ''} onClick={() => setView('guide')}>실습 가이드</button><button className={view === 'problem' ? 'on' : ''} onClick={() => setView('problem')}>문제가 생겼나요?</button><button className={view === 'terms' ? 'on' : ''} onClick={() => setView('terms')}>용어부터 배우기</button></nav><hr /><span className="eyebrow">나의 역할</span><button className={role === 'member' ? 'role on' : 'role'} onClick={() => setRole('member')}>하는 팀원입니다</button><button className={role === 'leader' ? 'role on' : 'role'} onClick={() => setRole('leader')}>하는 팀장입니다</button></aside><main><div className="toolbar"><label>검색 <input aria-label="가이드 검색" placeholder="예: Push, 충돌, 브랜치" value={query} onChange={(event) => setQuery(event.target.value)} /></label><small>진행 상태는 이 브라우저에 저장됩니다.</small></div>{view === 'guide' && <><section className="hero"><div><p className="eyebrow">{role === 'member' ? 'TEAM MEMBER PATH' : 'TEAM LEADER PATH'}</p><h2>{role === 'member' ? '팀원으로 첫 PR 보내기' : '안전한 작업 흐름 만들기'}</h2><p>실제 “팀원 소개 파일 추가” 프로젝트 변경 사례를 따라, 한 단계씩 진행합니다.</p></div><div className="progress"><b>{progress}%</b><small>나의 진행률</small><div><i style={{ width: `${progress}%` }} /></div></div></section>{searchMatches.length > 0 && <section className="search-results" aria-label="가이드 검색 결과">{searchMatches.map(({ item, index }) => <button key={item.id} onClick={() => { setStep(index); setQuery(''); }}>{index + 1}. {item.title} <small>{item.tool}</small></button>)}</section>}<div className="notice">표시된 번호와 초록색 강조 영역만 따라 누르세요. 모든 이미지는 개인 계정 정보가 보이지 않도록 마스킹한 참고 화면입니다.</div><div className="columns"><section><div className="section-title"><span><p className="eyebrow">{role === 'member' ? 'MEMBER JOURNEY' : 'LEADER JOURNEY'}</p><h3>{role === 'member' ? '팀원 과정' : '팀장 과정'}</h3></span><small>{list.length} steps</small></div>{list.map((item, index) => <button className={`row ${index === step ? 'current' : ''}`} key={`${role}:${item.id}`} onClick={() => setStep(index)}><span>{done.includes(`${role}:${item.id}`) ? '✓' : String(index + 1).padStart(2, '0')}</span><b>{item.title}<small>{item.tool} · {item.action}</small></b>›</button>)}</section><article className="card"><div className="meta"><span>{lesson.tool}</span> STEP {step + 1} / {list.length}</div><h3>{lesson.title}</h3><p className="lead">{lesson.action}</p>{practice[lesson.id] && <section className="practice"><p className="eyebrow">실제 프로젝트 변경 사례</p><b>{practice[lesson.id].change}</b><small>확인할 것: {practice[lesson.id].check}</small></section>}<Visual lesson={lesson} open={(src, alt) => setModal({ src, alt })} /><div className="facts"><div><b>왜 필요한가요?</b><p>{lesson.why}</p></div><div><b>성공 기준</b><p>{lesson.success}</p></div><div><b>자주 하는 실수</b><p>{lesson.mistake}</p></div><div><b>복구 방법</b><p>{lesson.recovery}</p></div></div><label className="check"><input type="checkbox" checked={done.includes(`${role}:${lesson.id}`)} onChange={toggleComplete} /> 이 단계를 완료했어요</label><div className="pager"><button disabled={step === 0} onClick={() => setStep(step - 1)}>← 이전</button><button disabled={step === list.length - 1} onClick={() => setStep(step + 1)}>다음 →</button></div></article></div>{role === 'member' && <Simulation />}</>}{view === 'problem' && <section className="page"><p className="eyebrow">TROUBLESHOOTING</p><h2>문제가 생겼나요?</h2><p>증상과 화면을 확인한 뒤, 안전한 해결 순서를 따르세요.</p><div className="cards">{problems.filter((problem) => problem.some(find)).map(([title, cause, fix, ask]) => <article key={title}><h3>{title}</h3><p><b>가능한 원인</b> {cause}</p><p className="fix"><b>안전한 해결 순서</b> {fix}</p><p><b>팀장에게 알릴 시점</b> {ask}</p></article>)}</div></section>}{view === 'terms' && <section className="page"><p className="eyebrow">GLOSSARY</p><h2>용어부터 배우기</h2><div className="terms">{terms.filter((term) => term.some(find)).map(([term, description]) => <article key={term}><b>{term}</b><p>{description}</p></article>)}</div></section>}<section className="downloads"><div><b>실습 템플릿</b><p>팀 규칙과 체크리스트를 Markdown으로 내려받아 저장소에 넣어 보세요.</p></div>{Object.entries(templates).map(([name, body]) => <button key={name} onClick={() => download(name, body)}>{name.replace('.md', '')} ↓</button>)}</section></main></div>{modal && <div className="modal" role="dialog" aria-modal="true" aria-label="스크린샷 확대 보기" onClick={() => setModal(null)}><div onClick={(event) => event.stopPropagation()}><button className="close" onClick={() => setModal(null)} aria-label="확대 이미지 닫기">×</button><img src={modal.src} alt={modal.alt} /></div></div>}</div>;
}
